-r requirements.txt
pytest>=8.0.0
# starlette's TestClient (full-stack websocket tests) rides on httpx.
httpx>=0.27.0
# Embedded Lua 5.1 (matches CC:Tweaked's Cobalt) — runs the player.lua test
# suite from pytest without a system Lua install.
lupa>=2.0
