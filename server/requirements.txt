fastapi>=0.111.0
uvicorn[standard]>=0.29.0
# [default] bundles yt-dlp-ejs (the JS challenge solver), so the deno runtime
# can solve YouTube's n/sig challenges offline — no runtime download, and the
# full (DASH) format list stays available instead of just HLS.
yt-dlp[default]>=2025.1.1
numpy>=1.26.0
# Compiles the per-cell image encoder (cc_encoder) to native code — ~6x the pure
# numpy core single-threaded.  0.61+ supports NumPy 2.x; pip resolves a mutually
# compatible pair.  If numba is ever absent the encoder falls back to numpy.
numba>=0.61.0
# Progress bars for tools/convert_to_ccmf.py (a batch CLI, not the live server).
tqdm>=4.66.0
# LZ4 block (de)compression for the CCMF `compression` field (spec §4.1.2): the
# server compresses payloads; the Lua/C++ clients ship their own block decoders.
lz4>=4.0.0
